{
  "name": "gutu-lib-ui-zone-static",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun run --cwd framework/libraries/ui-zone-static build",
    "typecheck": "bun run --cwd framework/libraries/ui-zone-static typecheck",
    "lint": "bun run --cwd framework/libraries/ui-zone-static lint",
    "test": "bun run --cwd framework/libraries/ui-zone-static test",
    "test:unit": "bun run --cwd framework/libraries/ui-zone-static test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
